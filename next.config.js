/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['th.bing.com','images.unsplash.com','fakestoreapi.com'], // Add the hostname here
  },
}

module.exports = nextConfig
